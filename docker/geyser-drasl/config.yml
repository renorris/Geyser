bedrock:
  address: 0.0.0.0
  port: 19132
  clone-remote-port: false

java:
  address: minecraft
  port: 25565
  auth-type: online
  custom-yggdrasil:
    enabled: true
    base-url: https://drasl.example.com
    allow-password-authentication: true

# Custom Yggdrasil automatically saves a refreshable token for each Bedrock XUID
# after the first successful password login. Passwords are never written to disk.

pending-authentication-timeout: 120
debug-mode: false
config-version: 8
